name: &name "candis"
version: &version "0.1.0"
app:
  lang: "en"
  encoding: "utf-8"
  title: !join [*name, " v", *version]
  color:
    primary: "#E91E63"
  routes:
    base: &base "/"
    api: &api !relurljoin [*base, "api"]
    data: &data !relurljoin [*api, "data"]
    files: !relurljoin [*data, "files"]
  container: "app"
  startdir: "data"
